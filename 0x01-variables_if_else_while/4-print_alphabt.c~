#include <stdio.h>

/**
 * main - prints all alphatical letters 
 * Return: 0 for success 
 */

int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		putchar('%c', letter);
	putchar('\n');
	return (0);
