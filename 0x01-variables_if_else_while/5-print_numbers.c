#include <stdio.h>

/**
 * main - prints out singke digits of base 10 from 0 to 9
 * Return: 0 for success
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
