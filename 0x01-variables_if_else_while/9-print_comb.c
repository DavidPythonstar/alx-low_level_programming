#include <stdio.h>

/**
 * main - prints single character combination of bsse 10 digits
 * Return: alway 0 for success
 */

int main(void)
{
	int i;

	for (i = 0; 1 < 10; i++)
	{
		putchar(i);
		putchar(',');
		putchar(' ');
	}
	putchar('\n');
	return (0);
}
