#include "main.h"

/**
 * _puts - counts letter in a strings
 * @str: string to count
 * Return: void
 */

void _puts(char *str)
{
	int i = 0;

	while (str[i] != '\0')
	{
		_putchar(str[i]);	
		i++;
	}
	_putchar('\n');
}
