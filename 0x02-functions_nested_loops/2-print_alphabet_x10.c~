#include "main.h"

/**
 * print_alphabet - prints all small case alphabets 10 x
 * Return: void
 */

void print_alphabet(void)
{
	char c;
	int i = 0;
	
	while (i < 10)
	{
		i++;
		for (c = 'a'; c <= 'z'; c++)
			_putchar(c);
		_putchar('\n');
	}
}
